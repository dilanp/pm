This folder contains start and stop scripts for Mac, PC, and Linux.

- start.sh: Runs frontend unit tests, builds the Docker image, and starts the container.
- stop.sh: Stops and removes the Docker container.
- start.ps1: Windows PowerShell version of the start script.
- stop.ps1: Windows PowerShell version of the stop script.